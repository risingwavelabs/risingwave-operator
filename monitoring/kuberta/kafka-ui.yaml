yamlApplicationConfig:
  kafka:
    clusters:
      - name: kstack
        bootstrapServers: kstack-kafka-headless.risingwave-operator-system:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false